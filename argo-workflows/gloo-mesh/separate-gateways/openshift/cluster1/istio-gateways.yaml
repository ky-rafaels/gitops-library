apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: istio-gateways
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: istio-gateways
    server: 'https://kubernetes.default.svc'
  source:
    repoURL: 'https://github.com/solo-io/gitops-library.git'
    targetRevision: workflows
    path: argo-workflows/gloo-mesh/separate-gateways/openshift/cluster1/istio-gateways
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true